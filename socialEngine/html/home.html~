{% extends "base.html" %}
{% block content %}
<div class="row">
<div class="col-sm-8">
    <ul class="list-group">
    {% for pub in pubs %}
        <li class="list-group-item">
            <div class="row">
                <div class="col-sm-11">
                    <h6>Posted at {{pub.pub_date}}<h6>
                    <h3>{{pub.firstName}} {{pub.lastName}}</h3>
                    <h4>{{pub.pub_text}}</h4> 
                </div>
            </div>
        </li>
    {% endfor%}
    </ul>
</div>
<div class="col-sm-4"></div>
</div>
{% endblock %}
